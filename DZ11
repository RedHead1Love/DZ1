using System;

class Program
{
    static void Main()
    {
        int startingNumber = 5;
        int step = 7;
        int maxNumber = 103;

        for (int currentNumber = startingNumber; currentNumber <= maxNumber; currentNumber += step)
        {
            Console.Write(currentNumber + " ");
        }
    }
}
